# claude-code-turn-recovery

Claude Code が API エラーでターンを打ち切られたとき、失われた作業を記録し、次のセッションの
冒頭で再開ブリーフィングとして提示するための最小構成サンプルです。詳細は
[README](./README.md) を参照。

配布物は `.claude/hooks/`(5つの hook と共有モジュール)・`.claude/settings.json`・
`.claude/tests/` の3つです。**このハーネスはプロジェクト固有の設定を持ちません。**
言語やディレクトリ構成に依存しないので、導入先で書き換える設定ファイルはありません。

## よく使うコマンド

```bash
# hook のテスト(外部依存なし)
python3 -m unittest discover -s .claude/tests -t .claude/tests -v

# 中断→再開の流れを手元で再現する(実運用の状態ディレクトリは汚さない)
bash examples/simulate-interruption.sh

# shellcheck(シェルスクリプトの静的検査)
shellcheck .claude/hooks/*.sh
```

## 変更時のルール

- **5つの hook が共有する契約を片方だけ変えない。** 状態ディレクトリのパス
  (`.claude/agent-calls/<session_id>/<prompt_id>/`)・`.call.json` / `.done.json` の
  ファイル名の組み立ては、書く側と読む側で完全に一致している必要があります。片方だけ
  変えると、ハーネスはエラーを出さずに「未完了 0 件」を返し続けます。
  `test_agent_call_ledger.py::LedgerContractTests` と
  `test_settings_contract.py` がこれを機械的に検査します。
- **未完了判定のロジックを hook 側に書き戻さない。** 2パス走査は
  `pending_agents_shared.py` の1箇所だけに置きます。以前これが2箇所にあったとき、片方
  だけ直した結果、中断記録と再開ブリーフィングで未完了の判定が食い違いました。
- **`.done.json` の集約はセッション全体で行う。** バックグラウンドのサブエージェントは
  発言をまたいで生き続けるため、`prompt_id` 単位で突き合わせると完了済みを未完了と
  誤認します。
- **上限で切る前に、必ず時刻で並べ替える。** `prompt_id` は UUID で発行順とは無関係です。
  辞書順のまま切ると、いま走っている呼び出しが枠から漏れます。
- **ブリーフィングの文言に、検証できない事実主張を書かない。** これは毎セッション自動で
  注入され、モデルには疑う手段がありません。行動指示は事実主張に依存せずに書けます。
- **`.call.json` は複数の hook が読む主データ。** 片方の読み手の都合の状態
  (提示済みフラグなど)を混ぜず、サイドカーのファイルを別に置きます。
- **すべての hook は fail-open。** 何が起きても `exit 0` で、`StopFailure` と
  `PostToolUse` 系は stdout に何も出しません。異常系で迷ったら、誤った記録を残す側では
  なく何も残さない側へ倒します。
- **シェルスクリプトは shellcheck を通す。** 抑制が必要な箇所は理由をコメントで書きます。

## 執筆・コミット

- ドキュメントは日本語の敬体で書きます。
- コミットメッセージは日本語で、なぜ変更したかを本文に残します。
